import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

const rendervar = (
  <div>
    <App />
  </div>
);

ReactDOM.render(rendervar, document.getElementById("root"));
